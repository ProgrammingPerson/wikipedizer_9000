# Environment Variables for Wikipedizer 9000
# Copy this file to .env and fill in your values

# ═══════════════════════════════════════════════════════════════════════════════
# FLASK CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════════

# Environment mode: 'development' or 'production'
FLASK_ENV=development

# Secret key for session encryption (REQUIRED for production)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# Server port
PORT=5001

# ═══════════════════════════════════════════════════════════════════════════════
# GOOGLE DRIVE INTEGRATION (Optional)
# ═══════════════════════════════════════════════════════════════════════════════
# To enable Google Drive integration, you need to:
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
# 2. Create a new project (or select existing)
# 3. Enable the Google Drive API
# 4. Create OAuth 2.0 credentials (Web application type)
# 5. Add authorized redirect URIs:
#    - For development: http://localhost:5001/oauth/callback
#    - For production: https://your-domain.com/oauth/callback
# 6. Copy the Client ID and Client Secret below

# Google OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Google OAuth 2.0 Client Secret
GOOGLE_CLIENT_SECRET=your-client-secret

# OAuth Redirect URI (for production, must match Google Cloud Console settings)
GOOGLE_REDIRECT_URI=https://your-domain.com/oauth/callback
